import { BrowserRouter, Routes } from 'react-router-dom'

const AppRouter = () => {
  return (
    <BrowserRouter>
      {/* Public route */}
      <Routes></Routes>
    </BrowserRouter>
  )
}

export default AppRouter
